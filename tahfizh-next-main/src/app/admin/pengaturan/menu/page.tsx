import { MenuSection } from "@/components/settings/settings-pages";

export const metadata = { title: "Pengaturan · Tampilan & Menu" };

export default function Page() {
  return <MenuSection role="ADMIN" />;
}
