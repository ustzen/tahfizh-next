import { ProfilSection } from "@/components/settings/settings-pages";

export const metadata = { title: "Pengaturan · Profil" };

export default function Page() {
  return <ProfilSection role="WALI_SANTRI" />;
}
