import { IdentitasSection } from "@/components/settings/settings-pages";

export const metadata = { title: "Pengaturan · Identitas Lembaga" };

export default function Page() {
  return <IdentitasSection />;
}
