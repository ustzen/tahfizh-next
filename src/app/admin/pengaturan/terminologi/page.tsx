import { TerminologiSection } from "@/components/settings/settings-pages";

export const metadata = { title: "Pengaturan · Terminologi" };

export default function Page() {
  return <TerminologiSection />;
}
