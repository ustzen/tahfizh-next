import { MenuIconSection } from "@/components/settings/settings-pages";

export const metadata = { title: "Pengaturan · Ikon Menu" };

export default function Page() {
  return <MenuIconSection role="DEVELOPER" />;
}
