import { KeamananSection } from "@/components/settings/settings-pages";

export const metadata = { title: "Pengaturan · Keamanan" };

export default function Page() {
  return <KeamananSection role="USTADZ" />;
}
